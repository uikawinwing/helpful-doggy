{
  "name": "helpful-doggy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env bridge/server.mjs",
    "check": "node --check bridge/server.mjs && node --check index.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20.6" }
}
